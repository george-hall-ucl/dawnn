% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dawnn.R
\name{generate_neighbor_labels}
\alias{generate_neighbor_labels}
\title{Generate a matrix of the labels of the 1,000 nearest neighbors of each cell.}
\usage{
generate_neighbor_labels(
  cells,
  reduced_dim,
  k = 1000,
  verbose = TRUE,
  label_names = "synth_labels",
  recalculate_graph = TRUE
)
}
\arguments{
\item{cells}{Seurat object containing the dataset.}

\item{reduced_dim}{String containing the name of the dimensionality
reduction to use.}

\item{k}{Integer number of neighbors to use (should be left as 1000 unless you have
a very good reason) (optional, default 1000).}

\item{verbose}{Boolean verbosity (optional, default = TRUE).}

\item{label_names}{String containing the name of the meta.data slot in
`cells' containing the labels of each cell (optional, default =
"synth_labels").}

\item{recalculate_graph}{Boolean whether to recalculate the KNN graph. If
FALSE, then the one stored in the \code{cells} object will be used (optional,
default = TRUE).}
}
\value{
A data frame containing the labels of the k nearest neighbors of
each cell.
}
\description{
Generate a matrix of the labels of the 1,000 nearest neighbors of each cell.
}
\examples{
\dontrun{
generate_neighbor_labels(cell_object, "pca")
}
}
