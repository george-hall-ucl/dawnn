% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dawnn.R
\name{run_dawnn}
\alias{run_dawnn}
\title{Identify which cells are in regions of differential abundance using Dawnn.}
\usage{
run_dawnn(
  cells,
  label_names,
  label_1,
  label_2,
  reduced_dim,
  n_dims = 50,
  nn_model = "~/.dawnn/dawnn_nn_model.h5",
  recalculate_graph = TRUE,
  alpha = 0.1,
  verbosity = 2,
  seed = 123
)
}
\arguments{
\item{cells}{Seurat object containing the dataset.}

\item{label_names}{String containing the name of the meta.data slot in
`cells' containing the labels of each cell.}

\item{label_1}{String containing the name of one of the labels.}

\item{label_2}{String containing the name of the other label.}

\item{reduced_dim}{String containing the name of the dimensionality
reduction to use.}

\item{n_dims}{Integer number of dimensions to use if computing graph
(optional, default 50).}

\item{nn_model}{String containing the path to the model's .hdf5 file
(optional, default "~/.dawnn/dawnn_nn_model.h5").}

\item{recalculate_graph}{Boolean whether to recalculate the KNN graph. If
FALSE, then the one stored in the \code{cells} object will be used (optional,
default = TRUE).}

\item{alpha}{Numeric target false discovery rate supplied to the
Benjamini–Yekutieli procedure (optional, default 0.1, i.e. 10\%).}

\item{verbosity}{Integer how much output to print. 0: silent; 1: normal
output; 2: display messages from predict() function.}

\item{seed}{Integer random seed (optional, default 123).}
}
\value{
Seurat dataset `cells' with added metadata: dawnn_scores (output of
Dawnn's model for each cell); dawnn_lfc (estimated log2-fold change in the
neighbourhood of each cell); dawnn_p_vals (p-values associated with the
hypothesis tests for whether a cell is in a region of differential
abundance; dawnn_da_verdict (Boolean output of Dawnn indicating whether it
considers a cell to be in a region of differential abundance).
}
\description{
\code{run_dawnn()} is the main function used to run Dawnn. It takes
a Seurat dataset and identifies which cells are in regions of differential
abundance.
}
\examples{
\dontrun{
run_dawnn(cells = dataset, label_names = "condition", nn_model =
"my_model.h5", reduced_dim = "pca", n_dims = 50, recalculate_graph = FALSE,
alpha = 0.2, verbosity = 0, seed = 42)
}
}
