# Package index

## All functions

- [`beta_method_of_moments()`](beta_method_of_moments.md) : Estimate the
  parameters of a beta distribution using the method of moments.
- [`determine_if_region_da()`](determine_if_region_da.md) : Determine
  whether each cell is in a region of differential abundance.
- [`download_model()`](download_model.md) : Download the neural network
  model used by Dawnn.
- [`generate_neighbor_labels()`](generate_neighbor_labels.md) : Generate
  a matrix of the labels of the 1,000 nearest neighbors of each cell.
- [`generate_null_dist()`](generate_null_dist.md) : Generate a null
  distribution of P(Condition_1) estimates.
- [`generate_p_vals()`](generate_p_vals.md) : Generate p-values for
  observed Dawnn model outputs.
- [`load_model_from_python()`](load_model_from_python.md) : Load the
  neural network model from its .hdf5 file.
- [`param_check()`](param_check.md) : Sanity check input parameters
- [`run_dawnn()`](run_dawnn.md) : Identify which cells are in regions of
  differential abundance using Dawnn.
